/* Thème: tranquilou – Plumes paisibles dérivantes */
import { Fx } from '../shared';
import { floatFx } from '../effects/floatFx';

export default function tranquilou(): Fx {
  return floatFx(['#a5d6a7', '#c8e6c9', '#e8f5e9', '#ffffff'], 'feather', 8, 25);
}
